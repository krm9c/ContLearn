{
    "__comment_guide": "See config/TEMPLATE.md for all available options and defaults",

    "__comment_data": "Dataset selection - auto-sets prob, problem, network, loss, metric",
    "data": "cifar10",

    "__comment_training": "Experimental parameters",
    "n_task": 10,
    "epochs_per_task": 100,
    "batch_size": 1024,

    "model_path": "outputs/cifar10_model",
    "save_iter": 10,

    "__comment_awb": "AWB architecture morphing disabled",
    "awb_enabled": false,

    "__comment_optimizer": "AdamW with weight decay for regularization",
    "optimizer": "adamw",
    "lr": 1e-3,
    "lr_schedule": "cosine",
    "weight_decay": 1e-4,

    "__comment_adaptive_lr": "Loss-based adaptive lr_min: boosts when loss ratio is high",
    "adaptive_lr_min_enabled": true,
    "lr_min_base": 1e-6,
    "lr_min_max": 1e-4,
    "lr_min_loss_ratio_threshold": 1.5,

    "__comment_warmup": "Task transition warmup: new task samples only, then experience replay",
    "task_warmup_enabled": true,
    "task_warmup_epochs": 5,
    "task_warmup_lr_factor": 0.1,
    "warmup_grad_weights": [1.0, 0.0, 0.0],

    "__comment_adaptive_grad": "Loss-based adaptive grad weights: shifts toward current task when loss ratio is high",
    "adaptive_grad_weights_enabled": true,
    "grad_weights_base": [0.3, 0.6, 0.1],
    "grad_weights_max_current": 0.7,
    "grad_weights_min_experience": 0.2,
    "grad_weights_loss_ratio_threshold": 1.2,

    "__comment_overrides": "Optional overrides (see TEMPLATE.md)",
    "debug_mode": false
}
