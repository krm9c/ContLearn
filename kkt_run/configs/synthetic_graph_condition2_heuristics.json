{
    "__comment": "CONDITION 2: Smoothness via Heuristics",
    "__comment_": "Fixed arch, cosine LR schedule, warm start",
    "data": "synthetic",
    "n_task": 10,
    "epochs_per_task": 500,
    "batch_size": 128,
    "awb_enabled": false,
    "lr_schedule": "cosine",
    "lr": 0.0001,
    "warmup_epochs": 50,
    "lr_warmup_factor": 0.1,
    "model_path": "kkt_run/results/synthetic_graph_condition2",
    "save_iter": 50,
    "grad_weights": [0.01, 0.98, 0.1],
    "debug_mode": false,
    "per_task_eval_enabled": true
}
