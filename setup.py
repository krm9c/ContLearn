"""Setup script for ContLearn package.

For development, install in editable mode:
    pip install -e .

For development with test dependencies:
    pip install -e ".[dev]"
"""

from setuptools import setup

# All configuration is in pyproject.toml
setup()
