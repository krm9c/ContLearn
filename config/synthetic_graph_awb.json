{
    "__comment_guide": "See config/TEMPLATE.md for all available options and defaults",

    "data": "synthetic",

    "n_task": 2,
    "epochs_per_task": 2,

    "model_path": "outputs/synthetic_graph_awb_model",

    "optimizer": "adamw",

    "__comment_awb": "AWB architecture morphing enabled",
    "awb_enabled": true,
    "awb_preliminary_epochs": 2,
    "awb_ab_training_epochs": 2,
    "awb_ab_warmup_epochs": 1,
    "awb_ab_max_iterations": 2,

    "__comment_arch_search": "keep this value equal to or greater than epochs_per_task, otherwise, architecture search is trivial because, an architecture initialized from scratch cannot keep up with an architecture that is being trained for epochs_per_task epochs",
    "arch_search_epochs": 2,
    "arch_search_lr": 0.001,
    "arch_search_max_iter": 2,

    "lr_schedule": "exponential",
    "debug_mode": true
}
